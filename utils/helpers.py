def format_response(response):
    """
    Format the response string to ensure it's clean and ready for display.

    :param response: str: The raw response string.
    :return: str: The formatted response.
    """
    return response.strip().capitalize()
